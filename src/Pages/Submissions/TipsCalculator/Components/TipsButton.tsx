export const TipsButton = ({ text }: { text: string }) => {
  return (
    <button className="h-[50px] w-[118px] rounded-lg bg-[#00474B] text-[24px] font-semibold text-white">
      ${text}
    </button>
  )
}
